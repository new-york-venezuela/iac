# This file is an example. The real inventory.ini is generated by Terraform
# (local_file resource in terraform/main.tf) and is gitignored.
#
# If you need to run Ansible without Terraform (e.g. existing server), create
# ansible/inventory.ini manually using this format:

[mailserver]
203.0.113.42 ansible_user=root volume_device=/dev/disk/by-id/scsi-0HC_Volume_12345678
